# Aura vNext environment example.
# Providers are configured in-app (Settings → Providers); no env vars are required.
# These are optional development conveniences only.

# Load the built renderer instead of the Vite dev server when running electron directly.
# AURA_LOAD_BUILT=1
